# 地牢回响：当前维护交接

> 本文件描述 **main 当前生产状态**，用于后续维护者或 AI 工程协作者快速接手。
>
> 旧 v6.x 合并、短 Profile 平衡探针、阶段性模拟结果等历史记录仍可在 Git 提交历史中追溯；它们不再作为当前产品合同。

## 1. 当前产品状态

- 当前公开版本：**v1.0.0**。
- 正式路线：**第 1 层 → 第 100 层**，生产入口不提供玩家自选起始深度。
- 在线试玩：`https://play.91hwl.cn/dungeon-echo/`
- 项目页面：`https://91hwl.cn/toys/dungeon-echo/`
- 运行形态：纯静态 HTML / CSS / JavaScript；核心游戏无 Node/PHP/API/数据库运行依赖。
- 存档：浏览器 `localStorage`，无云端账号/同步。
- 体验优先级：**PC 网页优先**，保留触控降级与浏览器 Gamepad API 支持。

## 2. 正式入口与开发夹具

### 生产入口

`index.html`

- 固定加载 `classic-100`；
- 新玩家从第 1 层开始；
- 正式长局通过已征服检查点减少重复，而不是让玩家直接跳过未完成内容。

### 开发入口

`dev.html`

- 保留 `classic-10` … `classic-60` 等短 Profile；
- 这些 Profile 是确定性回归/平衡夹具，不是正式游戏模式；
- 不要为了兼容旧测试重新把深度选择暴露给生产玩家。

## 3. 当前玩法骨架

### 四职业

- 战士：近战承压、横扫、反击/续航方向；
- 游侠：远程、走位、风筝和持续输出；
- 秘术师：脆弱、高爆发、技能冷却循环；
- 刺客：高风险近身爆发、位移与处决。

职业平衡目标不是完全相同胜率，而是每个职业都拥有清楚的优势、代价和解决问题的方法。

### 六栏装备

- 武器
- 护甲
- 头盔
- 靴子
- 戒指
- 项链

正式装备系统已经包含：

- 职业适配评价；
- 深层品质成长；
- 史诗/传说机制词缀；
- 内在价值与职业适配值分离的基础；
- +1…+5 锻造；
- +3 精炼路线选择；
- +5 淬炼完成。

不要把装备重新退化成“统一分数更高就自动更好”。

### 贪婪远征

核心循环：

`下潜 → 战斗/搜刮 → 判断是否继续 → 回城保住收益 → 整备 → 再次下潜`

死亡风险、回城卷轴、仓库/金库、补给、锻造、装备出售、幸运转盘和检查点共同组成长期风险收益循环。

### 城镇

当前已经有：

- 风险背包 / 安全仓库；
- 随身金币 / 安全金库；
- 有限补给；
- 阶段价格；
- 锻造；
- 已征服检查点；
- 幸运转盘与单格一次性结算政策。

下一步不是继续堆更多按钮，而是把这些服务组织成真正的长期进度枢纽。

## 4. 当前模块边界

- `game.js`：核心状态、地图、回合与必须位于引擎内部的战斗机制。
- `gameplay-tuning.js`：正式百层规则、职业和真人优先参数。
- `equipment-system.js`：装备生成、职业适配、深层成长、价值逻辑。
- `progression-system.js`：天赋与后续技能里程碑成长。
- `town-system.js`：检查点、城镇长期状态、轮盘政策。
- `commerce-system.js`：有限补给与阶段价格。
- `forge-system.js`：+3 精炼与 +5 淬炼。
- `content-system.js`：深层主题、守卫内容桥接与后续 Boss 状态机制。
- `desktop-controls.js`：桌面/手柄输入适配。
- `profiles/classic-100.profile.js`：正式内容与基础数据。
- `profiles/classic-10..60.profile.js`：开发/回归夹具。

模块化原则：**逐步拆分，不做大爆炸重写。**

如果机制必须直接参与回合/伤害/移动状态机，就允许修改 `game.js`；不要为了“模块数量更多”复制核心状态。

## 5. 当前发布验证

首个公开版本记录的确定性发布证据：

- production contract：24/24；
- 1→100 deterministic descent/victory：13/13；
- historical gameplay/save regression：525/525。

常用命令：

```bash
node --check game.js
node test/production.cjs
node test/descent100.cjs
node test/smoke.cjs
node test/release.cjs
```

`test/sim.cjs` 是平衡诊断工具，不是所有改动都必须运行的发布仪式。仅在战斗、掉落、成长、经济等相关改动时使用。

## 6. 当前最重要的未完成方向

优先级以 `PRODUCTION_ROADMAP.md` 与 Issues 为准，当前建议顺序如下。

### A. 十层守卫 / 终局 Boss 独立化

目前若干守卫仍然是共享 trait 的组合。目标是：

- 每十层节点拥有可记忆机制；
- 攻击有预警、阶段和反制；
- 至少若干 Boss 使用真正状态机；
- 100 层终局成为完整多阶段战斗。

### B. 城镇进度枢纽

把当前四栏式管理界面升级为：

- 商人/市集；
- 铁匠铺；
- 仓库/金库；
- 远征大门/制图台；
- 幸运转盘；
- 远征记录。

PC 端应允许直接点击服务，不要求角色为打开菜单而慢速走动。

### C. 20/40/60/80 技能里程碑

保持现有技能热键，通过里程碑选择改变行为，而不是再增加一排技能键。

### D. 真人 1→100 长局审计

重点观察：

- 回城时机；
- 资源压力；
- 金币竞争用途；
- 装备替换；
- 30 / 60 / 90 难度断点；
- 各职业强路线是否存在合理反制。

### E. 第二轮美术 / UI

优先处理：

- 四职业实战形象；
- 六栏装备与机制词缀反馈；
- 普通怪章节轮廓；
- 十位守卫/终局 Boss 独立形象；
- 城镇建筑/NPC/章节变化；
- Boss 读招、状态、粒子、音效；
- PC HUD、装备比较与危险信息层级。

视觉升级必须服务机制辨识，不要遮挡网格、路径和战斗信息。

## 7. 不要回退的决策

- 不恢复生产入口的自选起始层数；
- 不用 bot 深层低胜率作为连续全局削怪的理由；
- 不追求四职业完全一致的 DPS/生存/胜率；
- 不把百层成长变成十几个热键；
- 不为静态游戏引入没有明确产品价值的后端；
- 不为了“现代化”重写整个 `game.js`；
- 不把装备选择重新简化成只看一个总分。

## 8. AI 协作上下文

本项目允许并实际使用 AI 工程协作。OpenAI ChatGPT 已参与仓库审查、架构推理、调试、测试策略、玩法/经济分析、部署安全审查与文档整理。

接手项目的 AI/Agent 必须遵守：

1. 先读取真实仓库状态，再提出修改；
2. 优先选择最高效、最小回归面的实现；
3. 不为了显得“做了很多”运行与目标无关的大量测试；
4. 给出实际代码/文档改动，而不是只描述建议；
5. 保留存档兼容、正式入口和静态部署合同；
6. 对不确定事实进行验证，不用想当然覆盖现有实现；
7. AI 建议必须接受代码、测试或真人体验的验证。

当前 README 的公开说明中已透明记录 AI-assisted development；不要把这种协作描述成 OpenAI 官方背书。

## 9. 维护者接手时先看什么

建议顺序：

1. `README.md`
2. `PRODUCTION_ROADMAP.md`
3. 当前 open Issues
4. `RELEASE_NOTES_v1.0.0.md`
5. `DEVELOPMENT.md`
6. 相关模块代码
7. `BALANCE_NOTES.md`（需要平衡上下文时）

如果文档与代码冲突，以 **当前 main 代码 + 最新 Issues/提交事实** 为准，并更新文档消除冲突。
