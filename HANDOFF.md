# 地牢回响：基线合并交接（v6.3）

> 面向继续维护 `feature/dungeon-roguelike` 的模型与开发者。
> 当前基线：本地 `dungeon-echo-v6_2` 全量合并进本目录（工单 Issue #262，2026-08-24）。
> 交付形态：PC 网页优先；七档位深度（10/20/30/40/50/60/100）；经典模式 + 贪婪远征（城镇/仓库/锻造/死亡惩罚）。

## 1. 本轮改了什么

- 以 dungeon-echo-v6_2 为单一基线整体替换 game.js / index.html / style.css：
  内容 profile 化（`profiles/classic-*.profile.js` 七档，fail-closed 校验）、百层内容线、
  怪物特性（再生/爆炸/狂暴/远程）、木桶容器、贪婪模式城镇经济闭环、
  存档 schema v2 + 脏档逐字段修复、模式隔离。
- 旧基线独有回归合同全部移植进新套件（smoke 第 13 节），并按事实演进两处：
  - 图集合同从「1024×1024」更新为「宽 1024、高为 256 整倍格行」——v6.3 图集扩为 4×5（1024×1280）以容纳新增物品图标；
  - 引擎按 `naturalWidth/4` 取格，扩展行不影响既有渲染。
- 远端曾有的三个提交（拾取同步 b415349/023bb9e、沉浸全屏 9600ad7）在 v6_2 中均有等价实现，无功能回退；
  其行为断言已由第 13 节测试锁定。

## 2. 当前合同

| 项目 | 当前值 | 备注 |
|---|---|---|
| 地图 | 40×28 | 桌面全图视口不变 |
| 档位 | classic-10..60 / classic-100 | `?profile=` 或标题界面点选 |
| 最深层 | 各档 maxDepth；100 层终局 Boss 封锁楼梯 | 中层 Boss 不锁楼 |
| 手机视口 | 13–17 格自适应 | FOV=7 对齐 |
| 背包 | 12 格 | `bagcount` 同步刷新 |
| 图集 | 宽 1024 / 高 256×N（当前 N=5） | 4 列格、行可扩展 |

## 3. 验证

在 `games/dungeon-roguelike/` 运行：

```bash
node --check game.js
node test/smoke.cjs        # 199 项确定性回归（含仓库合同移植与新玩法/词缀/平衡/掉落/转盘/天赋扩充锁定）
node test/descent100.cjs   # classic-100 百层端到端胜利链路（13 项）
node test/sim.cjs          # 批量平衡模拟（SIM_DEPTH=classic-100 可选百层档）
```

## 3.2 六栏位扩展（2026-08-25）

- **新增 3 个装备栏**：武器/护甲/头盔/靴子/戒指/项链（原三栏 → 六栏）。
- 新基础表 `HELMET_BASES` / `BOOT_BASES` / `AMULET_BASES`（系统级常量，后续可迁移进 profile）。
- 锻造主属性：头盔 +def、靴子 +hp、项链 +crit；`genEquip` 六栏分发（w.30/a.25/h.15/b.15/r.10/n.05）。
- 存档/渲染/equipslot/select 全部扩展为六栏；新图层 `trinket` sprite。
- **待美术收尾**：图集第 5 行 12 个新图标格（helm/boot/amulet 系列）尚未绘制，渲染暂回退 0 号格。
- 测试：504/504（含六栏生成/穿戴/锻造合同）。

### 3.2.1 六栏位后的平衡基线（classic-10，SIM_MANY=30）

| 阶段 | 战士 | 游侠 | 法师 | 刺客 |
|---|---|---|---|---|
| 三栏（削弱后） | 33.3% | 26.7% | 53.3% | 50.0% |
| 六栏（怪物未调） | 76.7% | 63.3% | 93.3% | 96.7% ← 扩栏后玩家超速 |
| 六栏（怪已加深） | **40.0%** | 70.0% | 76.7% | 63.3% |

结论：六栏位让玩家战力高于三栏，已按新水平加深 classic-10 中后段怪（巨魔 38hp/恶魔 56hp/Boss 210hp）。死层分布回到 7–10 层终盘。游侠/法师 bot 已带技能但仍高于战士——跨职业公平对比仍待游侠风筝。

### 3.3 classic-30 中期死亡墙归因（2026-08-25，探针 + 修复后复测）

**工装缺陷（已修）**：`_probe.cjs` 旧探针的 bot 遇商店/神龛弹窗即卡死（`stuck-shop/shrine`），
classic-30 的 20 局中 19 局从未跑到真实结局——“存活曲线骤降”是卡死假象，不是死亡。
修复：引擎提炼 `closeShrine()` 并在 DE_TEST 暴露 `closeShop/buyShop/closeShrine/getShopStock`；
两个 bot 均正确处理 shop/shrine 状态（sim bot 另带拟人购物：药水补到 4 瓶、残血包扎）。
注意：sim 寻路把 NPC 当障碍绕行，永远不会进店，故旧 sim 基线无卡死污染，classic-10 基线 40/70/76.7/63.3 复测不变。

**classic-30 修复后真实胜率**（SIM_MANY=30，不进店）：战士 **0%**、游侠 **0%**、法师 10%、刺客 23.3%，
死层集中 15–29。死亡归因（探针 100 局，9 死）：致命一击 **远程×7 / 近战×2**；
死者手握 23–61 瓶药水——不是不喝药，是喝药也扛不住。

**成因结构（按权重）**：
1. **远程怪浓度**：16 层起 38%→27 层 70%（霜语法师/巫妖/陨星使入池），远程伤害 = atk×0.8 − def/2，
   防御对远程天然减半，且多只远程同回合集火，每回合承伤 30–60；
2. **怪物攻击跳涨**：d11（10.6→12.8）与 d17（19.1→20.6）新怪入池造成台阶；
3. **玩家装备断供**：eqAtk 在 11 层后停滞 ~8.4、21 层回落 7.1（掉落稀缺 + 评分门槛），
   def ~11–19 对 19–25 攻击的怪已无减免意义；
4. **药水回复比例衰减**：回复 = 14+2×层（d20≈54 ≈ 最大血量 15%），重伤再减半——消耗战必输；
5. **bot 无威胁规避**：顶着弹幕按预算捡装备/走向楼梯，放大以上全部压力（91/100 局超时也是被风筝，探针无追猎模式）。

结论：**不是单一“怪太强”，而是“远程浓度 × 攻击台阶 × 装备断供”在中期叠加**。classic-10 无此结构，基线不变。

**候选调整方向（待作者裁决，未实施）**：
- 压平 classic-30 d11–17 的怪物攻击台阶（或推迟新怪入池）；
- 每层远程怪数量上限（如 ≤3 只）；
- 中期装备追赶：提高 11–20 层掉落评分/词缀成长，让 eqAtk 不停摆；
- 药水回复改为与最大血量挂钩（如 18% pMaxHp）而非固定 14+2×层。

### 3.4 机制修复（2026-08-25，作者裁决：调代码不调怪）

纯数值压怪五轮后近战仍趋近 0%，确认是**职业机制缺口**而非怪物数值问题，按作者指示转向引擎机制修复：

1. **战士被动「坚甲」**：`pDef` 天生附加 `1 + floor((lvl-1)/5)` 扁平减伤——直接修正近战"每杀挨 2 次反击"的消耗数学；
2. **游侠被动「灵巧」**：近战攻击 10% 闪避（`monsterAttack` 前置判定；不挡远程，保留克制面）；
3. **药水回复保底**：`heal = max(14+2×层, 18% pMaxHp)`（potionBoost/重伤乘数不变）——修掉"深层药水越来越不解渴"的结构性衰减；
4. **横扫 cd 7→6**。

配套：DE_TEST 暴露 `pDef`；职业 blurb 同步；classic-30 profile 同时保留了平滑化调整（弱怪 max+2、
中阶怪 atk -1~-2、武器线补 10@10 / 13@17 两档、armorBases/ringBases 中期上调、
baseMonsterCount 6→5 + maxMonsters 20→14、eliteChance 0.16→0.13、eliteAtkMult 1.35→1.25、
击杀掉药 0.68→0.74（gold 0.55→0.60 以满足 §17 带宽合同）、层掉落 equip1/2 0.45/0.28→0.52/0.34）。
注：`monsterPerDepth` 有整数校验（isInt），不能设小数——降怪密度走 baseMonsterCount/maxMonsters。

验证：`test/_mech.cjs` 四项断言全过（坚甲 1/5、法师 0、药水 18% 保底生效、1000 次近战闪避 103 次、cd=6）；
smoke 504/504。classic-30 / classic-10 胜率重测见 §3.5。

### 3.5 机制修复后的新基线（SIM_MANY=30，2026-08-25）

| 职业 | classic-30 修复前 | classic-30 修复后 | classic-10 旧基线 | classic-10 重测 |
|---|---|---|---|---|
| 战士 | 0% | **20.0%** | 40.0% | 76.7% |
| 游侠 | 0% | 10.0% | 70.0% | 56.7% |
| 法师 | 63.3% | 70.0% | 76.7% | 76.7% |
| 刺客 | 70.0% | 80.0% | 63.3% | 63.3% |

- classic-30 死亡层从 14–20（中期墙）推移到 19–30：**中期墙已消除**，剩余死亡属终局难度；
- 战士 20% 为 bot 不撤退/不进店下的结果，人类玩家更高；游侠 10% 受 bot 不会风筝限制（见 §5 已知限制）；
- classic-10 战士 40%→76.7%：坚甲+药水保底在浅层收益大，属机制修复的预期外溢，暂不再压回（避免重入数值循环）；
- descent100 13/13、smoke 504/504 全绿。

后续候选（未实施）：游侠 bot 风筝逻辑、战士 bot 低血脱离、classic-20/40/50/60/100 同款中期体检。

### 3.6 全档位体检与 40–100 平滑批次（2026-08-25）

**体检基线（修复机制后、平滑前，SIM_MANY=30）**：

| 档位 | 战士 | 游侠 | 法师 | 刺客 | 死亡层 |
|---|---|---|---|---|---|
| classic-20 | 20.0% | 6.7% | 43.3% | 46.7% | 14–20（可接受） |
| classic-40 | 0% | 0% | 13.3% | 20.0% | 14–32 |
| classic-50 | 3.3% | 0% | 0% | 6.7% | 17–30 |
| classic-60 | 0% | 0% | 0% | 0% | 14–27（全灭墙） |
| classic-100 | 0% | — | — | — | 15–22（仅战士完成） |

**结论**：40/50/60/100 与旧 classic-30 共享同一套未平滑 1–30 层怪与武器断层，患同款中期墙。

**处置**：把 classic-30 验证过的配方套用到 40/50/60/100 的 1–30 层带（31+ 扩展层与各档 Boss 不动）：
弱怪延池（骷髅/兽人/幽魂/血教徒 max+2）、中阶怪软化（恶魔 13→12、怨灵 12→11、霜法 14→13、
魔像 48hp/14→42/13、血爵 40/16→36/14、巫妖 18→15 且射程 3→2、憎恶 60/17→52/16、
龙裔 55/20→48/19、陨星使 48/22→44/20、虚空幼体 21→20）、武器线补 10@10 / 13@17 两档、
baseMonsterCount 6→5、maxMonsters 22/24/26/30→16/18/20/24、eliteChance→0.13/0.14、
eliteAtkMult→1.25/1.3、击杀掉药→0.74（gold 0.60 保 §17 带宽）、击杀掉装→0.83、层掉落 equip 上调。
classic-20 保持原样（体检数值可接受，避免过度平滑）。

图集批次同步完成：12 枚六槽新图标（头盔/靴子/项链 ×4 阶）绘制进 5–7 行，
图集扩为 1024×2048（8 行），CSS 精灵定位公式同步（400% 800%、行距 14.285714%），
rows 0–4 逐像素 round-trip 校验一致，smoke 504/504。

验证扫描结果见 §3.7。

### 3.7 40–100 平滑后验证（SIM_MANY=30，2026-08-25）

| 档位 | 战士 | 游侠 | 法师 | 刺客 | 死亡层变化 |
|---|---|---|---|---|---|
| classic-40 | 0→**10.0%** | 0% | 13.3→**23.3%** | 20→**46.7%** | 14–25 → 17–31（终盘化）✓ |
| classic-50 | 3.3% | 0% | 0%（19/30 超时存活） | 6.7→**20.0%** | 17–30 → 15–32（后移） |
| classic-60 | 0% | 0% | 0% | 0→3.3% | 14–27 → 16–33（后移） |
| classic-100 | 0% | 0% | 0% | 0% | 14–27 → 14–38（后移） |

**判定**：
- **14–20 层的中期悬崖在所有档位已消除**——死亡分布全部后移至战役中后段，classic-40 进入健康带；
- classic-50/60/100 对 bot 仍近乎不可通：50+ 层长线战役的核心技能（进店补给、残血脱离、
  风筝走位、跨 50 层资源规划）bot 一概不会，bot 胜率不再能代表人类可达上限。
  按作者「调代码不刷 bot」的裁决，**停止数值继续压怪**；
- 后续正确路径（待作者裁决）：① bot 升级（进店/撤退/风筝）作为工装工程任务；或
  ② 作者实机试玩 50+ 档位给出人类基准，再决定是否需要结构改动（如 31+ 层装备追赶线）。

### 3.8 bot v3：补给/撤退/风筝（2026-08-25，工装工程任务①）

sim.cjs bot 四项技能升级（作者批准方向）：
1. **主动进店**：药水 <2 瓶或血量 <55% 时寻路到商人撞门购物（药补到 4 瓶、<60% 血买包扎）；
   空手而归设 120 回合冷却防开-关死循环，换层重置；
2. **顺路用营地**：血量 <70% 时撞向未使用的余烬营地（免费包扎 45%）；
3. **残血脱离**：被贴脸且血量 <35% 时走 retreatStep（四方向选离最近怪更远的可走格，拉不开距离不撤）；
4. **游侠风筝**：游侠被贴脸且血量 <60% 即脱离（移动可能触发直线自动射击）；
5. **喝药阈值修正**：45% pMaxHp（原 45% hpBase，装备血量加成不再导致喝药过晚）。

验证扫描（7 档位全重测）结果见 §3.9。

### 3.9 bot v3 最终基线与关键 bug 修复（2026-08-25）

**调试中发现并修复 bot 原生死循环**：技能伤害判定 `adj.some(m => m.hp < before)` 中 `before` 是
**贴脸怪血量总和**，拿单怪血量和总和比在 2+ 怪贴脸时恒真——疾步空转（朝向被挡、无伤害、无 CD）
却被误判为"已造成伤害"→ 无限 continue，永不近战直到超时。已改为逐怪 before/after 对比
（战士横扫块同款隐患一并修复）。该 bug 旧版就存在，v3 的撤退行为制造了大量
"走廊两怪追尾"场景使其爆发（游侠超时一度 21/30）。

**bot v3 最终基线（SIM_MANY=30）**：

| 档位 | 战士 | 游侠 | 法师 | 刺客 |
|---|---|---|---|---|
| classic-10 | 76.7% | 50.0% | 76.7% | 66.7% |
| classic-20 | 20.0% | 0% | 50.0% | 40.0% |
| classic-30 | 26.7% | 6.7% | 56.7% | 66.7% |
| classic-40 | 10.0% | 0% | 23.3% | 43.3% |
| classic-50 | 0% | 0% | 0% | 20.0% |
| classic-60 | 0% | 0% | 0% | 0% |
| classic-100 | 0% | 0% | 0% | 0% |

- 游侠超时从 21/30 收敛到 0–3/30，全部战斗到终盘（不再挂机空转）；
- classic-10/20/30/40 全职业有胜率、死亡层分布于战役后段——中期墙在所有 ≤40 层档位消除；
- classic-50/60/100 对 bot 仍不可通：50+ 层的资源规划（跨层金币/药水/仓库策略）超出 bot 能力，
  胜率不代表人类上限；后续如需校准，走作者实机基准或更强的 bot（不再压怪数值）。

## 4. 下一步候选

1. 真实桌面浏览器过一遍完整贪婪循环（出发→打→T 回城→存→锻造→再出发→故意死一次）；
2. 美术批次（大脑）：城镇 UI 收尾、图集第 5 行图标统一风格、可选归一化回 1024²；
3. 玩法深化：已征服层付费快速下潜、保险符（付费保死亡背包）、装备词缀扩充。

## 5. 已知限制

- 未部署；浏览器人工 E2E 仍需作者实机走查。
- sim.cjs 的 bot 策略较弱，胜率仅作平衡参考，不代表玩家可达上限。
- 存档单槽位、按档位区分；切换档位会隔离旧档（有测试锁定该行为）。
## 3.10 城镇观感批次：真转盘 + 城镇场景横幅
- 幸运转盘从一排文字 chips 改为 canvas 圆盘：8 扇区按奖品类别着色、固定指针；
  抽奖缓动减速至少四整圈落在中奖格（数据结算保持同步，smoke 同步断言不受影响），
  中奖格金色脉冲高亮 1.6s，重置时快速甩盘 620ms；动画期间抽奖/重置按钮禁用。
- town-screen 顶部新增 #town-scene 夜景横幅 canvas：星空闪烁、月、远山剪影、街道、
  五座功能建筑（仓库/锻造炉+火星/市集摊/客栈挂灯/转盘帐篷）与下方功能区一一对应，
  窗灯摇曳 + 前景篝火动画；rAF 循环仅城镇状态运行，headless stub 下自动退化为单帧。
- 验证：node --check 通过；smoke 504/504；descent100 13/13。
