# 地牢回响：百层正式版路线图

> 目标：最终部署到网站供游客直接游玩。玩法、平衡、内容先定型，美术/UI 最后统一收尾。
> 正式玩家路线只有一条：第 1 层开始，持续深入至第 100 层。`classic-10..60` 仅保留为内部测试 Profile。

## 一、当前生产原则

1. **单一百层旅程**：不再把 10/20/30/40/50/60/100 当玩家可选模式。
2. **真人体验优先**：bot 用于发现死亡墙、死循环、经济崩坏，不用于强行拉平职业胜率。
3. **构筑优先于数值**：装备、天赋、技能应形成不同打法，而不是只让评分越来越大。
4. **不先做美术重构**：视觉资产、统一 HUD、最终动效在玩法稳定后集中收尾。
5. **静态部署友好**：保持原生 HTML/CSS/JS，无服务端依赖，可由 Nginx 直接托管。

## 二、代码边界（渐进式拆分，不大爆炸重写）

当前 `game.js` 仍是稳定运行核心。后续新功能按以下边界放置，旧逻辑只在需要修改时逐步迁移：

- `game.js`：核心状态机、地图、回合调度；逐步瘦身。
- `desktop-controls.js`：键盘/手柄/PC 输入适配，最终只保留输入职责。
- `gameplay-tuning.js`：正式百层规则、职业参数、生产玩法开关。
- `equipment-system.js`：装备生成、词缀、职业价值、锻造/重铸、传奇效果。
- `progression-system.js`：天赋、技能升级、百层里程碑。
- `town-system.js`：仓库、商店、金库、锻造、回城与已征服深度快速返回。
- `profiles/classic-100.profile.js`：正式内容与数值数据。
- `profiles/classic-10..60.profile.js`：内部测试夹具，不进入游客选择界面。

不会一次性拆完；每次只迁移正在修改的模块，并保持可运行版本。

## 三、优先级 P0：必须先解决的玩法问题

### 1. 正式路线统一为 1→100

- 标题页不再提供深度选择。
- 正式入口默认/固定使用 `classic-100`。
- 现有 `J` 快速下潜不能用于跳过未征服层；最终改为“仅返回已征服深度”的快速旅行。
- 存档继续记录当前层与最高征服层。

### 2. 装备系统从“评分换装”升级为“构筑选择”

当前问题：
- 六栏位已经存在，但八类词缀总体仍过于通用；
- 同一个总评分对不同职业价值完全不同；
- 稀有度主要代表“多几个随机属性”，没有真正改变打法；
- 锻造只有固定主属性 +1~+5，决策深度不足。

正式设计：
- 武器：输出核心，职业专属副属性与后续武器特性。
- 护甲：生存核心，但避免战士只能无脑堆 DEF；增加 HP/回复/反伤路线。
- 头盔：防御、回复、抗负面方向。
- 靴子：生命、机动/脱离方向。
- 戒指：暴击、吸血、收益型构筑的主要竞争槽。
- 项链：技能、暴击与高风险高收益方向。
- 金币词缀保留，但明确属于“牺牲战力换经济”的贪婪路线。

下一阶段：
- 职业适配评分，不再只显示统一总评分；
- 避免无意义重复词缀；
- 深层提高高品质装备的可见度；
- 史诗/传说加入机制词缀，而非只叠数字；
- 锻造后增加一次可控的副属性选择/重铸，而不是无限随机洗。

### 3. 重做随机破甲

当前规则：防御高于攻击越多，随机破甲概率越高，最高 45%。这会惩罚战士正确地构筑防御。

目标：
- 不再用高概率随机全额穿甲惩罚高防；
- 改成可理解、可预判的反高防机制，例如特定敌人的“蓄力破甲/护甲削弱”状态；
- 普通怪不应因为玩家防御高就凭空更容易穿甲。

## 四、优先级 P1：100 层长期可玩性

### 1. 天赋成长

当前每 3 级从通用天赋池抽 3 个，且允许重复。百层下会出现：
- 同一天赋重复堆叠；
- 扁平减伤/暴击/技能急速后期失控；
- 四职业最后选出的东西非常相似。

目标：
- 通用天赋 + 职业天赋分层；
- 同一核心天赋有限等级；
- 每职业至少 2–3 条清晰路线。

建议路线：
- 战士：坚壁反伤 / 吸血狂战 / 横扫控场。
- 游侠：远射暴击 / 机动风筝 / 击杀回复猎手。
- 秘术师：奥术爆发 / 冷却控制 / 生命换伤害玻璃炮。
- 刺客：暴击斩杀 / 吸血连杀 / 影袭位移。

### 2. 技能成长

四职业目前各只有一个主动技能，100 层过长。

目标：
- 不一定增加大量按键；优先让现有技能在关键层数发生分支升级。
- 例如 20/40/60/80 层获得一次技能强化选择。
- 每次强化改变行为，而不是只提高伤害数字。

### 3. Boss 独特机制

当前 10 层一个中层 Boss 的节奏是对的，但 Boss 多数仍依赖通用怪物 AI 和数值。

目标：
- 10/20/.../90 + 100 层终局 Boss 各至少一个独有机制；
- 机制应能被观察和应对，而不是突然随机秒杀；
- Boss 奖励与该阶段新构筑能力绑定。

## 五、优先级 P2：百层内容与经济

### 内容密度

- 现有 100 层有大量怪物与主题，但深层仍以数值换皮为主。
- 需要按约 10 层一个“章节”组织：敌人行为、环境规则、Boss、奖励形成一组。
- 80–100 层必须有新的玩法压力，而不只是更高 HP/ATK。

### 城镇与回撤

- 贪婪远征应逐步成为主体验；经典模式可后续定位为挑战/速通模式。
- 回城卷轴继续承担“贪还是撤”的核心选择。
- 快速旅行只允许返回已征服层，不能购买跳过新层。
- 仓库/金库/锻造需要围绕装备构筑形成明确资源循环。

### 经济

需要重点检查：
- 1–100 层金币收入与锻造成本曲线；
- 出售装备是否造成无限滚钱；
- gold/plunder 词缀的机会成本是否足够；
- 药水/回城卷轴是否在后期仍是有意义的消耗品；
- 保险符是否把死亡惩罚稀释过度。

## 六、优先级 P3：体验与工程

### 新手引导

- 前 1–5 层用实际遭遇教学，不依赖大段说明。
- 第一次装备、第一次商店、第一次回城分别解释一次即可。

### 保存与上线

- 保持 localStorage 本地存档；游客不登录也能继续。
- 存档升级必须保留版本迁移/脏档修复。
- HTML 不长缓存；带版本号的 JS/CSS/图集可长缓存。
- 最终可挂载到例如 `/games/dungeon-echo/`，或由 `play.91hwl.cn` 链接进入。
- 不需要 Node/PHP/API 才能运行核心游戏。

### 性能

正式上线前再做：
- 100 层长局存档体积；
- `localStorage` 同步写入是否产生卡顿；
- Canvas / 粒子在低性能 PC 的帧时间；
- 页面切后台/恢复与手柄断连。

## 七、最后阶段：美术/UI 收尾

在 P0–P2 玩法稳定后统一处理：

- 装备图标与六栏位缺失图；
- 各职业主角外观差异；
- 10 个阶段的主题视觉统一；
- Boss 独立形象与技能反馈；
- HUD 信息层级、装备比较、构筑标签；
- 城镇场景、按钮、字体、动效、音效统一；
- PC 1920×1080 / 1366×768 主体验，随后确认手机降级布局。

## 八、完成定义

正式上线前至少达到：

1. 玩家不能从标题页选择跳过层数；
2. 新角色从 1 层到 100 层有一条完整可存档旅程；
3. 四职业各有至少 2 条可辨识构筑路线；
4. 装备选择不能退化成只看一个统一评分；
5. 10 个 Boss 节点有可辨识玩法差异；
6. 30/60/90 层没有明显数值断崖；
7. 死亡、回城、存仓、锻造形成稳定风险收益循环；
8. 键鼠与手柄完成核心流程；
9. 玩法定型后再完成最终美术/UI；
10. 可作为纯静态目录直接部署至 Nginx。